

echo hello
  //This is the command(echo) you enter into the shell, it will return an output of "hello"

echo "hello"
  //This returns a string("hello") as we have input an arguement with quotations marks("_")
echo "YOUR NAME"

expr 32 - 8
  //This command ("expr") can be used for basic computations
  //REMEMBER to separate all numbers with a space

expr 3500 \* 12

clear
